{
  "name": "gc-electron",
  "version": "1.0.0",
  "description": "game core electron app",
  "scripts": {
    "dev": "webpack --config webpack.dev.js && electron ./dist/main.js",
    "build": "webpack --config webpack.prod.js",
    "build-dev": "webpack --config webpack.dev.js",
    "clean": "rm -rf dist",
    "purge": "rm -rf node_modules",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch-electron": "webpack --config webpack.prod.js --watch",
    "watch-electron-dev": "webpack --config webpack.dev.js --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^30.1.0",
    "ts-loader": "^9.5.1",
    "typescript": "^5.4.5",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4",
    "webpack-merge": "^5.10.0"
  },
  "dependencies": {
    "electron-is-dev": "^3.0.1"
  }
}
